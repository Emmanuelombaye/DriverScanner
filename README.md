# DriverScanner
A modern, offline-capable Windows app built with React + Electron that scans drivers using PowerShell/WMI, analyzes error messages with local AI (DistilBERT), enables one-click rollback with pnputil, and supports dark/light mode via Tailwind CSS. Fully offline — no cloud dependencies. Packaged as a single .exe.
